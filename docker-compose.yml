 
version: '3'

services:
  db:
    image: mysql
    environment:
      MYSQL_DATABASE: 'ptarmigan'
      MYSQL_USER: 'ptarmigan'
      MYSQL_PASSWORD: 'password'
      MYSQL_ROOT_PASSWORD: 'password'
    ports:
      - 3306:3306
    expose:
      - 3306